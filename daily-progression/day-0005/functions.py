def add(a, b):        # Def means "define (a function"  ;  add is the name   ;   (a, b) is the argument
    result = a + b      #tells it what to do
    return result      # important part: must write "return results"

answer = add(3, 5)
print(answer)    #its 8
